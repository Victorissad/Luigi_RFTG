<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark">

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <!-- Logo ou titre de l'application - Style camping été -->

    <TextView
        android:id="@+id/tvTitreApp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:fontFamily="sans-serif-medium"
        android:text="🎬 RFTG"
        android:textColor="@color/camping_forest"
        android:textSize="38sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:textColor="@color/camping_earth"
        android:textSize="16sp"
        android:textStyle="italic" />

    <!-- Champ email -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Email"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/camping_forest"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="votre.email@exemple.com"
        android:inputType="textEmailAddress"
        android:padding="16dp"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/camping_earth"
        android:background="@drawable/retro_edittext_background"
        android:layout_marginBottom="24dp" />

    <!-- Champ mot de passe -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mot de passe"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/camping_forest"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="••••••••"
        android:inputType="textPassword"
        android:padding="16dp"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/camping_earth"
        android:background="@drawable/retro_edittext_background"
        android:layout_marginBottom="32dp" />

    <!-- Sélection du serveur -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Serveur"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/camping_forest"
        android:layout_marginBottom="8dp" />

    <Spinner
        android:id="@+id/spinnerURLs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:background="@drawable/retro_edittext_background"
        android:layout_marginBottom="12dp"
        android:entries="@array/listeURLs" />

    <EditText
        android:id="@+id/etURLCustom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="URL personnalisée (optionnel)"
        android:inputType="textUri"
        android:padding="16dp"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/camping_earth"
        android:background="@drawable/retro_edittext_background"
        android:layout_marginBottom="32dp" />

    <!-- Bouton de connexion avec gradient camping -->
    <Button
        android:id="@+id/btnLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Se connecter"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="18dp"
        android:background="@drawable/retro_button_gradient"
        android:textColor="@color/white" />

    <!-- ProgressBar pour le chargement -->
    <ProgressBar
        android:id="@+id/progressBarLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:indeterminateTint="@color/camping_sage"
        android:visibility="gone" />

    <!-- Message d'erreur -->
    <TextView
        android:id="@+id/tvErreurLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textColor="@color/camping_terracotta"
        android:textSize="14sp"
        android:textStyle="bold"
        android:visibility="gone" />

</LinearLayout>

</ScrollView>
